$schema: ../../.moon/cache/schemas/project.json

language: typescript
layer: library

project:
  name: sax
  description: Simple API for XML in JavaScript

tasks:
  bench:
    command: bun
    local: true

  build:
    command: bun build.ts
    inputs:
      - src/**/*
      - build.ts
    outputs:
      - dist/**/*
